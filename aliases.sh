#!/usr/bin/env bash

alias docker_build="docker-compose build"
alias docker_up="docker-compose up -d"
alias docker_down="docker-compose down"

alias docker_composer="docker-compose run composer"
alias docker_artisan="docker-compose exec php php artisan"
alias docker_phpunit="docker-compose exec php vendor/bin/phpunit"

